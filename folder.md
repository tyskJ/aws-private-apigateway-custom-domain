# フォルダ構成

- フォルダ構成は以下の通り

```
.
└── envs
    ├── config
    │   ├── dummy
    │   └── private-apigateway-parameter.yml      PRIVATE-APIGATEWAY スタック用パラメータファイル (Ignore Git)
    └── private-apigateway.yml                    PRIVATE-APIGATEWAY スタック定義ファイル
```
